const host = 'http://localhost:5858' //本地开发
// const host = 'https://uwl4ev8j.qcloud.la'

const config = {
    host,
    loginUrl: `${host}/musicSoul/login`,
    userUrl: `${host}/musicSoul/user`
}

export default config